#ifndef UTIL_H
#define UTIL_H

#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include <stdbool.h>

bool check_IP(char* IP);
bool check_port(char* port);
bool check_net(char* net);

#endif